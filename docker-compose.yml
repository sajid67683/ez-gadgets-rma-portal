services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # This is PERFECT. It matches your new app.py exactly.
      - ./instance/ezgadgets_rma.db:/app/instance/ezgadgets_rma.db
    environment:
      - FLASK_ENV=production